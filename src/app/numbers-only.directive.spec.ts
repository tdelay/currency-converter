import { NumbersOnly } from './numbers-only.directive';

describe('NumbersOnlyDirective', () => {
  it('should create an instance', () => {
    // const directive = new NumbersOnly();
    // expect(directive).toBeTruthy();
  });
});
